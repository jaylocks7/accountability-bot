// ponytail: stub — Phase 2 will rewrite this handler fully per PLAN.md §3-§5
import { APIGatewayProxyEvent } from "aws-lambda";

export async function handleWebhook(_event: APIGatewayProxyEvent): Promise<void> {
    throw new Error("handleWebhook not yet implemented — Phase 2");
}
